// list of files / patterns to load in the browser
files = [
  JASMINE,
  JASMINE_ADAPTER,
  'appengine/static/js/**/*.js',
  'test/*.js'
];


// list of files to exclude
exclude = [
  
];


// level of logging
// possible values: LOG_DISABLE || LOG_ERROR || LOG_WARN || LOG_INFO || LOG_DEBUG
logLevel = LOG_INFO;